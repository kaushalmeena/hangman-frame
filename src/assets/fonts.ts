import { readFileSync } from "fs";

export const playpenSans400FontData = readFileSync(
  `./public/fonts/PlaypenSans-400.ttf`
);

export const playpenSans500FontData = readFileSync(
  `./public/fonts/PlaypenSans-500.ttf`
);
